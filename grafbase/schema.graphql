type Record @model @search {
  eventTime: DateTime!
  eventType: EventType!
  productId: Int!
  categoryId: Int!
  categoryCode: String!
  brand: String!
  price: Float!
  userId: Int!
}

enum EventType {
  VIEW
  CART
  REMOVE_FROM_CART
  PURCHASE
}
