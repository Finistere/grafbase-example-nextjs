extend schema @postgres(
    name: "brabier",
    url: "postgres://Finistere:ScF6KrVBJ7jU@ep-rough-sun-97600315.eu-central-1.aws.neon.tech/neondb"
  )

type Player @model @search {
  name: String! @unique
  notes: [Note]
}

type Note @model @search {
  note: String!
  player: Player!
}

# extend type Query {
#   hello(name: String): String! @resolver(name: "hello")
# }

