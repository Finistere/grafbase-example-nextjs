extend schema @postgres(
    name: "brabier",
    url: "postgresql://Finistere:k7VM1dYGNQFs@ep-round-shadow-11016698.us-east-2.aws.neon.tech/neondb"
  )

type Player @model @search {
  name: String! @unique
  notes: [Note]
}

type Note @model @search {
  note: String!
  player: Player!
}

# extend type Query {
#   hello(name: String): String! @resolver(name: "hello")
# }

